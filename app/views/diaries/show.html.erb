<h1>Nueva acción</h1>

  <p>
  	<%= 'Fecha:' %>
  	<%= @diary.date %>
  </p>

  
  # f.label :Planta
  # f.collection_select(:plant_ids, Plant.order(:name), :id, :name)
  

  <p>
  	<%= 'Acción:' %>
  	<%= @diary.action %>
  </p>
  <p>
  	<%= 'Observaciones:' %>
  	<%= @diary.observation %>
  </p>
